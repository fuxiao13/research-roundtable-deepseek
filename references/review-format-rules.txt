Output JSONL only: one JSON object per line, with `id`, `severity`, `category`, `blocking_effect`, `anchor`, `evidence`, and `action`. IDs use D1, D2, ... and are unique. `severity` is MUST_FIX or RECOMMENDED; Normal modes allow MUST_FIX only. `category` is one of engineering_feasibility, scientific_validity, statistical_validity, publication_viability, execution_risk, safety, reproducibility, data_integrity. `blocking_effect` is one of blocks_execution, blocks_publication, presentation_only, non_blocking_improvement. Output exactly NO_MATERIAL_CHANGE if none. No Markdown, prose, or code fences.
